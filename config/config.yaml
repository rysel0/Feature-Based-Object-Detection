paths:
  query_dir: "data/query"
  scene_dir: "data/scenes"
  output_dir: "data/output"

detector:
  method: "sift"
  nfeatures: 1000

matcher:
  type: "bf"
  ratio_threshold: 0.7
  norm_type: "L2"

homography:
  ransac_threshold: 5.0
  min_matches: 5